<h2>Your Rhythm</h2>
<div class="heatmap">
  {{#each signals.hourDistribution}}
    <div class="heatmap-cell" style="background: rgba(255,255,255,{{#if count}}0.{{percentBar count}}{{else}}0.05{{/if}})" title="{{formatHour hour}}: {{count}} commits"></div>
  {{/each}}
</div>
