<!DOCTYPE html>
<html lang="{{language}}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <meta name="description" content="{{description}}">
  <meta property="og:title" content="{{title}}">
  <meta property="og:image" content="{{ogImage}}">
  <style>{{{styles}}}</style>
</head>
<body>
<div class="container">
  <h1>{{periodLabel}}</h1>
  <p style="color:var(--muted);margin-bottom:2rem;">{{subtitle}}</p>
